import { styled } from "styled-components";

export const Wrapper = styled.section`
    width: 100%;
    padding-top: 4.25rem;

    .content {
        max-width: 900px;
        margin: 0 auto;
    }
`